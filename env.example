# Base URL of the Django HRMS backend (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000

# Optional: force the service layer to always fall back to the bundled mock
# data instead of hitting the backend. Handy for local UI work.
# VITE_USE_MOCKS=true
